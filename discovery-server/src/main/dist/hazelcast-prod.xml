<?xml version="1.0" encoding="UTF-8"?>
<hazelcast xsi:schemaLocation="http://www.hazelcast.com/schema/config hazelcast-config-3.6.xsd"
           xmlns="http://www.hazelcast.com/schema/config"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <properties>
        <property name="hazelcast.rest.enabled">false</property>
        <property name="hazelcast.wait.seconds.before.join">0</property>
        <property name="hazelcast.logging.type">slf4j</property>
    </properties>

    <group>
        <name>prod</name>
        <password>prod-password</password>
    </group>

    <network>
        <port auto-increment="true" port-count="10000">5701</port>
        <outbound-ports>
            <!--
                Allowed port range when connecting to other nodes.
                0 or * means use system provided port.
            -->
            <ports>0</ports>
        </outbound-ports>
        <join>
            <!--
                 Deployments on AWS *MUST* use this as AWS does not support IPv4 multicast.
            -->
            <aws enabled="true">
                <iam-role>dwc-discovery</iam-role>
                <region>us-east-1</region>
                <security-group-name>discovery-internal-dev</security-group-name>
            </aws>
            <tcp-ip enabled="false">
            </tcp-ip>
            <multicast enabled="false">
            </multicast>
        </join>
        <ssl enabled="false"/>
        <socket-interceptor enabled="false"/>
    </network>

    <partition-group enabled="false"/>

    <executor-service name="default">
        <pool-size>16</pool-size>
        <queue-capacity>0</queue-capacity>
    </executor-service>

    <!-- BEGIN: INTERNAL VERT.X DATA STRUCTURES -->
    <map name="__vertx.subs">
        <backup-count>1</backup-count>
        <time-to-live-seconds>0</time-to-live-seconds>
        <max-idle-seconds>0</max-idle-seconds>
        <eviction-policy>NONE</eviction-policy>
        <max-size policy="PER_NODE">0</max-size>
        <eviction-percentage>25</eviction-percentage>
        <merge-policy>com.hazelcast.map.merge.LatestUpdateMapMergePolicy</merge-policy>
    </map>

    <semaphore name="__vertx.*">
        <initial-permits>1</initial-permits>
    </semaphore>
    <!-- END: INTERNAL VERT.X DATA STRUCTURES -->

</hazelcast>