rootProject.name = name

include("${rootProject.name}-core")
project(":${rootProject.name}-core").name = "${rootProject.name}-core"

include("${rootProject.name}-web")
project(":${rootProject.name}-web").name = "${rootProject.name}-web"