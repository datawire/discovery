rootProject.name = name

include("${rootProject.name}-common")
project(":${rootProject.name}-common").name = "${rootProject.name}-common"

include("${rootProject.name}-server")
project(":${rootProject.name}-server").name = "${rootProject.name}-server"