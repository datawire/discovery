apply plugin: 'java'

sourceCompatibility = 1.8
targetCompatibility = 1.8

ext {
  sparkVersion = '2.3'
}

repositories {
  jcenter()
}

dependencies {
  compile(
      "com.sparkjava:spark-core:${sparkVersion}",
      project(':hub-client')
  )
}