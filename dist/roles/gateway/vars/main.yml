---
# file: gateway/vars/main.yml

# fully qualified name of the package
package_fqn: "{{ package_base_name }}-{{ package_version }}"

# filename of the package
package_archive: "{{ package_fqn }}.tgz"

# path to the package archive on the system
package_archive_path: "/tmp/{{ package_archive }}"